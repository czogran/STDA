clear all
f=power(10,6);
% r=0.1:0.1:10;
% bsr=5*r;

 r=sqrt(2.5/pi);
 bsr=2;
f=5e4:

param=zeros(1,length(r));
for i=1:length(r)
    obj=CableClass(f,r(i),bsr(i));
%     if(obj.damping<50)
        param(i)=obj.decDamping;
%     end
end
plot(param)
min(param)
max(param)